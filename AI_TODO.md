# TODO

## 执行规则

- 任务一旦开始，你不需要人类介入，必须完成已制定的自测脚本，直到前后端开发完成，交付人类最终测试。
- 实现过程中，尽量经常的打印当前任务的实现进度百分比，以便人类观察
- 为了快速完成任务，不要一开始读取所有todo项的上下文，逐个读取和完成
- 完成一项勾选一项，勾选时要检查是否有新的TODO，有就继续完成。
- 完成全部TODO后询问是否提交，要分批提交，提交代码时不含此文件！！！

## 笔记

- 含表格的页面：角色权限/操作审计/关怀提示/部门管理/岗位管理/用户列表/文件管理/业务管理下的所有页


## 任务列表

为避免遗忘上方规则，请每次开始前输出“我已熟知规则”，以示记忆良好。

- [x] 后端权限控制面：为角色增加稳定 `role_code/is_protected` 标识，为菜单权限增加 `is_delegable` 标识并补增量 DDL
- [x] 后端统一授权：新增超管识别、受保护角色识别、权限范围比较与 `RequireSuperAdmin`，禁止依赖用户名/角色名判断
- [x] 后端权限收口：岗位绑角色、角色绑菜单、菜单权限定义改为超管控制面，Service 层必须再次校验操作者
- [x] 后端 API 拆分：岗位资料与岗位角色分离，用户资料/调岗/状态/密码重置分离，避免夹带权限字段
- [x] 后端目标规则：禁止非超管自调岗、自改状态、批量包含自己，以及操作受保护或同级/更高权限账号
- [x] 后端即时生效：移除无 TTL 的长期权限缓存，权限检查实时读取数据库并补齐权限变更后的会话处理
- [x] 后端回归测试：覆盖岗位绑超管、自调岗、新建超管账号、重置高权限账号、菜单权限标识篡改和直接 API 绕过
- [x] 后端文档：同步权限、组织、认证 API 文档和安全约束说明
- [x] 后端前端能力契约：当前用户返回 `permission_keys/is_super_admin`，用户、岗位、角色列表返回可操作能力，菜单树返回可委派属性
- [x] 前端鉴权基础：同步 OpenAPI/类型/Auth Store，提供统一 `hasPermission/isSuperAdmin` 判断
- [x] 前端组织接入：拆分用户资料/调岗/状态/密码接口，拆分岗位资料/角色配置交互，禁止用户名保护判断
- [x] 前端权限接入：角色使用 `role_code/is_protected`，菜单使用 `is_delegable`，控制面仅超管显示
- [x] 前后端测试与文档：覆盖能力字段、接口载荷、按钮显隐和目标操作，完成变更文件定向 lint、typecheck、test、build（不执行 check:all）
- [x] 前端角色摘要：岗位管理“绑定角色”和用户列表“关联角色”纵向最多展示 3 个，超出部分通过悬浮查看
- [x] 前端驻留标签：支持拖拽调整标签顺序，并补充排序状态单测与拖拽落点反馈
- [x] 前端驻留标签运行时：Vite 强制 React 单实例解析，避免 dnd Hook dispatcher 为空
- [x] 个人资料组织信息：`/account/me/profile` 返回部门、岗位、角色，并在“我的信息”弹窗展示
- [x] 用户岗位级联：无直属岗位的子部门继承最近上级部门岗位，新增/调岗校验保持一致
- [x] 岗位级联权限反馈：不可分配岗位保留展示并置灰标注，避免误判为岗位数据为空
- [x] 权限种子角色：`04_permission_tables.sql` 新增普通员工角色，默认不授予后台菜单权限
- [x] 角色列表排序：`GET /permission/roles` 无显式排序时默认按 `id ASC`
- [x] 角色可见范围提示：普通管理员角色筛选区显示橙色星号说明，并修正本地非受保护角色标识
- [x] 权限种子精简：移除安全管理员、业务运营管理员及其岗位/菜单/用户关联
- [x] 角色可见范围提示：使用白底清晰且非黑灰的深赤橙主题色
- [x] 初始化岗位角色：直接优化基线 SQL，移除运营专员/审计专员并将普通业务岗位改绑普通员工
- [x] 管理岗位角色：在原始幂等 SQL 中为所有主管/经理额外绑定部门主管，并清理已合并的冗余 SQL
- [x] model_sql_pg 清理：审计全部初始化文件，删除历史/占位冗余 SQL，仅保留必要建表与功能种子
- [x] 部门种子精简：仅保留集团总部及 10 个一级部门，删除二级小组 seed
- [x] 部门成员数去冗余：删除 organization_department.member_count 持久化字段，保留 API 实时统计
- [x] 前端登录体验：用户名或密码输入时按 Enter 可提交，并防止重复登录请求
- [x] 前端登录状态：点击或回车后按钮持续 loading 到页面跳转，失败时恢复
- [x] 菜单权限提示：筛选区向非超管说明仅超管可编辑及防止越权的原因
- [x] 可委派说明：菜单权限列表和编辑表单通过问号解释 is_delegable 的授权边界
- [x] 岗位人数提示：关联人数/在岗人数标题改用问号帮助图标
- [x] 受保护标记说明：岗位和用户列表改用“保”徽章悬浮说明，移除重复星号提示
- [x] 星号说明组件：封装 UiAsteriskHint 并替换全部页面内重复实现
- [x] 受保护徽章：封装红色“保”字徽章，替换岗位和用户列表的“受保护”标签并增加悬浮说明
- [x] 用户名规则与交互：列表显示 15 字符并支持复制；创建/导入用户名须字母开头且仅含字母数字下划线减号
- [x] 新增用户岗位说明：所属岗位标签右侧解释“无权分配”的权限边界
- [x] 角色保护基线：仅超级管理员角色受保护，其余初始化角色全部改为非保护
- [x] 用户删除提示：移除软删除说明末尾的“不可恢复”
- [x] 驻留标签拖拽性能：降低激活距离、使用 DragOverlay，并缩短换位动画
- [x] 权限设计文档：详细记录现有权限体系及岗位管理权分离方案
- [x] 转移岗位说明：所属岗位标签右侧解释“无权分配”的权限边界
- [x] 岗位管理层级：按权限设计文档增加 management_rank，并将用户/岗位管理与分配切换为管理权判断
- [x] 岗位管理层级展示：现有岗位管理表格增加只读管理层级列和简明说明
- [x] 新增岗位管理层级：超管新增岗位时可填写层级示例值，普通管理员固定为 0 且后端防绕过
